---
// Tools
import { type ImageMetadata } from "astro";
import { getRatio } from "@/lib/helpers";

// Compositions
import Frame from "../compositions/Frame.astro";

// Components
import MyPicture from "../ui/MyPicture.astro";
import ImageGrid from "./ImageGrid.astro";

interface Props {
  src: ImageMetadata;
  alt?: string;
  orientation?: "landscape" | "portrait" | "wide" | "square";
}

const { src, alt = "Project image", orientation = "landscape" } = Astro.props;

const ratio = getRatio(orientation);
---

<ImageGrid>
  <Frame
    ratio={ratio}
    class="col-12 tablet-s:col-10 col-start-1 tablet-s:col-start-2"
  >
    <!-- Rendered w: 1160, double: 2320 -->
    <MyPicture src={src} alt={alt} />
  </Frame>
</ImageGrid>
